# Дорожная карта приложения

Этот файл — источник истины для согласованных следующих доработок приложения.
Завершённые этапы остаются в `docs/stage-*.md`, а решения и ограничения из этого
документа не должны молча изменяться при последующей работе. Изменение принятого
решения требует отдельного согласования с пользователем.

Текущий активный блок: **надёжность, читаемость дерева и управление приложением
после v0.3.0, редакция от 25.08.2026**. Предыдущие этапы и решения сохранены
ниже как исторический контекст и не отменяются этим блоком.

## Правила работы по дорожной карте

- Сначала проектировать формат данных, миграцию и проверку целостности, затем
  менять интерфейс и визуализацию.
- Никогда не удалять сведения из существующих `.familytree` при миграции. Старый
  файл должен оставаться читаемым, а перед изменением формата создаётся резервная
  копия и проверяется восстановление.
- Приложение остаётся local-first: без интернета можно создавать, редактировать,
  сохранять и открывать дерево. Внешние сервисы являются необязательными
  помощниками, а не источником истины.
- Сведения, выведенные автоматически, не выдавать за подтверждённые. Для них
  хранить происхождение, статус предположения и возможность исправления.
- Каждая операция со связями должна быть атомарной: обновить каноническую связь,
  обратные представления, проверить инварианты и только потом обновить экран.
- Для каждого этапа обязательны модульные тесты, сценарии последовательного
  создания дерева, проверка открытия сохранённого файла и визуальная проверка.
- При обнаружении противоречия приложение должно объяснить его пользователю и
  предложить исправление; неизвестные сведения сами по себе не являются ошибкой.

## Активный блок после v0.3.0

Этот блок добавлен по результатам анализа скриншотов и проекта
`1 первый тест.familytree`. Скриншоты являются свидетельствами состояния
интерфейса, а не отдельными инструкциями. В файле проекта обнаружены формат 7,
13 человек, 20 канонических связей и 4 брака. Реальный семейный файл нельзя
добавлять в репозиторий как тестовый fixture: для автоматических тестов нужен
обезличенный проект с такой же структурой связей.

### Активный приоритет 0 — отсутствие зависаний и безопасное сохранение

#### P0.1 Геокодер: завершение запроса и актуальный адаптер

- Добавить тайм-аут запроса, отмену через `AbortController` и отмену предыдущего
  запроса при новом поиске или закрытии редактора.
- Гарантировать сброс состояния загрузки через `finally`, включая зависший,
  отменённый и исключительный сценарии.
- Игнорировать устаревший ответ, если пользователь уже изменил адрес или начал
  новый поиск.
- Разделять ошибки неверного ключа, лимита запросов, сети, пустого результата и
  ручного режима.
- Проверить актуальный endpoint Яндекса и адаптер ответа; предпочтительно
  использовать текущий `/v1`, `lang=ru_RU` и `Address.Components`, сохраняя
  совместимость с прежней структурой ответа.
- Сохранить ручной ввод адреса полностью рабочим без сети и без API-ключа.

Критерий готовности: ни один сетевой сценарий не оставляет кнопку в состоянии
«Ищем…» навсегда; запрос завершается за ограниченное время; пользователь всегда
понимает, можно ли продолжить ручной ввод.

Статус: **завершён 25.08.2026**. Реализованы тайм-аут, отмена, защита от
устаревших ответов, отдельные ошибки ключа и лимита, актуальный endpoint и
современные компоненты ответа Яндекса. Формат `.familytree` не менялся.

#### P0.2 Быстрое сохранение в прежний файл

- Надёжно запоминать путь после открытия или первого сохранения через нативный
  Electron-диалог, а не полагаться только на `file.path` HTML-инпута.
- Назначить быстрое сохранение на `Ctrl+S` для Windows; буквальное `Win+S` не
  считать основным сочетанием, поскольку оно конфликтует с системным поиском.
- Перед перезаписью создавать резервную копию и записывать файл атомарно через
  временный файл с проверкой результата.
- Если файл перемещён, удалён или папка недоступна, предложить «Сохранить как» и
  объяснить причину, не теряя текущие изменения.
- Не менять поведение «Сохранить копию» и архивирования.

Критерий готовности: `Ctrl+S` повторно сохраняет открытый проект в его прежнее
место, сбой записи не повреждает основной файл, а недоступный путь приводит к
понятному безопасному сценарию восстановления.

Статус: **завершён 25.08.2026**. Нативное открытие сохраняет абсолютный путь,
`Ctrl+S` повторно использует его, запись выполняется через проверенный временный
файл, предыдущая версия получает боковую копию `.backup`, а недоступный путь
переводит пользователя в безопасный сценарий «Сохранить как». Формат
`.familytree` не менялся.

#### P0.3 Безопасный запуск Electron при сбое GPU

- Добавить явный аргумент `--safe-mode` и alias `--software-rendering`.
- Отключать аппаратное ускорение до `app.whenReady()`, чтобы приложение могло
  открыться при падении GPU-процесса или повреждённом драйвере.
- Для локальной диагностики поддержать переменную
  `FAMILY_TREE_SOFTWARE_RENDERING=1`.
- Объяснять пользователю в меню и статусной строке, что включён программный
  рендеринг и приложение может работать медленнее.
- Не включать режим постоянно по умолчанию: исправление драйвера остаётся
  предпочтительным решением.

Критерий готовности: приложение запускается с `--safe-mode` без рабочего GPU,
пользователь видит понятный статус, обычный запуск не теряет аппаратное
ускорение, а формат `.familytree` не меняется.

Статус: **реализован 25.08.2026**. Реализованы оба аргумента запуска,
диагностическая переменная окружения, статус через preload/IPC и пояснение в
интерфейсе.

#### P0.4 Надёжная проверка обновлений

- Всегда публиковать `latest.yml` вместе с Windows-установщиком и `.blockmap`.
- Не показывать пользователю stack trace, HTTP-заголовки и внутренние пути при
  ошибке updater.
- Для ошибки 404 дать понятное действие: открыть страницу релиза или повторить
  проверку позже.

Статус: **реализован 26.08.2026**. Текущая ошибка updater сокращается до
понятного сообщения, а текущему релизу требуется добавить отсутствующий asset
`latest.yml`.

### Активный приоритет 1 — корректная геометрия и читаемость связей

#### P1.1 Границы карточек и концы линий

- Ввести единый контракт геометрии: источник линии, точка выхода, точка входа,
  безопасный канал и минимальный зазор.
- Привязывать концы линий к фактическому border-box карточек, учитывая крупный
  текст, длинные ФИО, место рождения, дополнительные поля и ручное перемещение.
- Проверить вертикальные линии родитель–ребёнок, горизонтальные линии партнёров,
  несколько родителей, несколько детей, сворачивание ветви и режим родственной
  ветви.
- Не допускать входа линии внутрь карточки, зазора между линией и карточкой и
  ложного утолщения из-за совпадающих каналов.

Критерий готовности: вертикальные и горизонтальные линии начинаются и
заканчиваются на border-box карточек с учётом фактической ширины и высоты,
ручного смещения и крупного текста.

Статус: **завершён 25.08.2026**. Добавлен единый контракт границ и anchor-точек,
рендер карточки использует те же размеры, что и геометрия, а hover больше не
сдвигает карточку относительно линии. Формат `.familytree` не менялся.

#### P1.2 Маршрутизация и разнесение подписей

- Разделить геометрию линии и размещение подписи.
- Для каждой подписи учитывать размер текста, карточки, линии и соседние
  подписи.
- Ввести горизонтальные и вертикальные каналы подписей с минимальным зазором.
- Для плотного участка использовать перенос, компактную подпись или раскрытие
  по клику; не обрезать смысловое название связи.
- Сохранить короткую подпись на полотне и полное описание по клику/фокусу.
- Не позволять одинаковым подписям «Родство» сливаться на общей шине.
- Отдельно увеличить минимальное пространство для подписи брака между супругами.
- Проверить отображение подписей в PDF, PNG и TIFF так же, как на полотне.

Критерий для проекта-примера: брак Александра и Эвелины читается полностью,
подписи родительских связей не накладываются, а линии видны от границы одной
карточки до границы другой.

Статус: **завершён 25.08.2026**. Добавлен общий расчёт размеров и каналов
подписей с учётом карточек и соседних подписей; увеличен минимальный зазор
между карточками партнёров; полотну и экспорту передана одна и та же раскладка.
Формат `.familytree` не менялся.

#### P1.3 Тестовая матрица плотного дерева

- Создать обезличенный fixture с четырьмя поколениями, супругами, несколькими
  детьми, несколькими родителями, длинными именами и крупным текстом.
- Проверять обычный и крупный текст, разные ширины правой панели, режим полного
  дерева и родственной ветви.
- Добавить геометрические проверки пересечений карточек, линий и подписей.
- Отдельно провести ручную визуальную проверку на реальном файле пользователя,
  не сохраняя его в репозитории.

Статус: **завершён 25.08.2026**. Добавлен обезличенный fixture с четырьмя
поколениями, несколькими родителями и партнёрствами, длинными данными и
режимом крупного текста. Автоматически проверяются пересечения карточек,
маршрутов и подписей, полная/родственная ветвь и открытая/закрытая правая
панель. Реальный файл пользователя открыт только для ручной визуальной
проверки и не добавлен в репозиторий.

### Активный приоритет 2 — техническая чистота интерфейса и ввод дат

#### P2.1 Скрыть идентификаторы от пользователя

- Убрать ID человека, ID связей и технические идентификаторы из обычной правой
  панели и списков родственников.
- Оставить идентификаторы в канонической таблице связей, сохранённом файле,
  операциях БД, миграциях и проверках целостности.
- При необходимости предусмотреть отдельный диагностический режим только для
  разработчика, не включённый для обычного пользователя.

Критерий готовности: пользователь видит понятные имена и типы связей, а проект
после сохранения и повторного открытия не теряет технические ключи.

Статус: **завершён 25.08.2026**. Секция идентификаторов и строки ID связей
удалены из обычной правой панели и списков родственников. Технические ключи
сохранены в канонической таблице `relations`, сохранении, миграциях и операциях;
регрессионный тест подтверждает сохранение и повторное открытие ключей. Из
пользовательских диагностических сообщений также убраны значения технических
идентификаторов. Следующий этап — P2.2.

#### P2.2 Центрирование кнопки сворачивания

- Заменить Unicode-символ стрелки на иконку из используемой библиотеки.
- Зафиксировать геометрический и оптический центр иконки внутри кнопки.
- Проверить положение кнопки в обычном и крупном тексте, при наведении, фокусе и
  свёрнутом состоянии.

Статус: **завершён 25.08.2026**. Unicode-стрелки заменены на библиотечные
`CaretRight` и `CaretDown`; для обычного и крупного текста заданы отдельные
размеры кнопки и SVG-иконки, а центрирование закреплено отдельным контейнером.
Регрессионный тест проверяет раскрытое и свёрнутое состояния, фокус и стили.
Следующий этап — P2.3.

#### P2.3 Маска точной даты рождения

- В режиме «Точный день» показывать маску `__.__.____` и принимать только цифры,
  автоматически добавляя точки.
- Поддержать корректную вставку даты из буфера и предсказуемое движение курсора.
- На blur проверять реальный календарный день, месяц, год и високосные годы.
- Хранить дату в существующем каноническом формате, не вводя отдельный формат
  только ради UI-маски.
- Сохранить режимы «Только год», «Примерно», «Диапазон» и «Неизвестно».

Критерий готовности: пользователь вводит точный день только цифрами, а
неполный ввод не уничтожает черновик и получает понятную ошибку рядом с полем.

Статус: **завершён 25.08.2026**. Цифровая маска `__.__.____` подключена в
редакторе основной информации и мастере добавления человека. Ввод и вставка
очищаются до цифр, точки добавляются автоматически, положение курсора сохраняется
относительно цифр, а blur-валидация проверяет реальный календарный день и
високосные годы. Каноническое хранение и остальные режимы точности не изменены.
Регрессионные тесты покрывают маску, курсор, неполный ввод и оба UI-потока.
Следующий этап — P3.2.

### Активный приоритет 3 — полотно и система горячих клавиш

#### P3.1 Масштабирование колесом мыши

- Перевести колесо над полотном на изменение масштаба.
- Масштабировать относительно точки курсора, чтобы дерево не «прыгало».
- Сохранить панорамирование перетаскиванием и добавить понятное поведение для
  `Shift+колесо`, если потребуется горизонтальная прокрутка.
- Проверить колесо мыши, тачпад, карточки, мини-карту и правую панель отдельно.

Статус: **завершён 25.08.2026**. Колесо над полотном изменяет масштаб в диапазоне
55–135% относительно точки курсора; `Shift+колесо` и горизонтальное колесо
тачпада перемещают полотно по горизонтали. Перетаскивание, кнопки и клавиши
масштаба сохранены, а мини-карта и правая панель не перехватываются обработчиком
полотна. Добавлены модульные и интеграционные проверки. Следующий этап — P3.2.

#### P3.2 Пользовательские горячие клавиши

- Создать единый реестр команд: сохранение, открытие, сохранение копии, отмена,
  повтор, поиск, масштаб, центрирование, отображение всей ветви и переключение
  режима дерева.
- Добавить в настройки просмотр, изменение и сброс сочетаний.
- Проверять конфликты между командами и предупреждать о системных сочетаниях.
- Показывать краткую инструкцию по сочетаниям прямо в настройках.
- По умолчанию использовать `Ctrl+S`, `Ctrl+Z`, `Ctrl+Y`, `Ctrl+F`, `+`, `-` и
  стрелки; не перехватывать глобальные сочетания Windows без отдельного решения.
- Сохранять настройки горячих клавиш как необязательные данные проекта с
  безопасными значениями по умолчанию для старых файлов.

Статус: **завершён 25.08.2026**. Команды переведены на единый реестр; в настройках
доступны изменение, сброс отдельных сочетаний и сброс всех значений. Конфликты
блокируют сохранение и объясняются пользователю, сочетания с клавишей Windows не
принимаются, а потенциально системные сочетания сопровождаются предупреждением.
Настройки сохраняются в необязательном `project.settings.shortcuts`, поэтому
старые `.familytree` открываются со стандартными значениями без миграции версии.
Добавлены инструкция и регрессионные тесты. Блок P0–P3 завершён; следующий этап
требует отдельного обновления дорожной карты.

Критерий готовности: все команды имеют один источник обработки, конфликты
объясняются пользователю, старые проекты открываются с рабочими стандартными
сочетаниями.

### Сквозные предложения и ограничения

- Не использовать реальный семейный файл как автоматический fixture из-за
  персональных данных; применять обезличенную структуру, а реальный файл —
  только для локальной ручной проверки с согласия пользователя.
- Добавить регрессионные проверки для зависшего геокодера, потери пути сохранения,
  атомарной записи, плотных подписей, крупного текста и повторного открытия.
- Проверять не только экран, но и экспорт, потому что линии и подписи являются
  общей моделью визуализации.
- В каждом внешнем сервисе сохранять local-first поведение: сеть помогает, но
  не становится источником истины и не блокирует ручное заполнение.
- Сохранять отдельный диагностический путь для разработчика вместо вывода
  технических данных обычному пользователю.
- Не увеличивать версию формата без необходимости. Если настройки горячих
  клавиш или новые данные требуют изменения схемы, сначала описать миграцию,
  резервную копию и проверку совместимости.

### Definition of Done для активного блока

Каждый подэтап закрывается отдельно и только после: реализации, модульных и
сценарных тестов, проверки старых `.familytree`, повторного сохранения и
открытия, визуальной проверки обычного и крупного текста, проверки отмены и
восстановления, production-сборки и документации в `docs/stage-*.md`.

Для каждого подэтапа создаются отдельные commit и push. Релиз или изменение
версии приложения выполняется только по отдельному прямому запросу пользователя.

## Исторический план предыдущего блока

Следующие приоритеты описывают ранее согласованный блок расширения связей,
компоновки дерева и семейных сведений. Они сохраняются для контекста и не
перезаписываются активным блоком после v0.3.0.

## Принятые решения по модели данных

### Структурированное ФИО

Не вводить правило «ФИО обязательно состоит ровно из трёх слов». Хранить имя
структурированными частями: фамилия, имя, отчество и дополнительные варианты
имени при необходимости. Все части необязательны. На экране формировать
отображаемое ФИО из заполненных частей. Валидация проверяет символы, длину,
управляющие символы и логические противоречия, но не запрещает неполные
исторические записи, двойные фамилии, инициалы и неизвестных людей.

### История фамилий

Вместо одного поля девичьей фамилии поддерживать историю изменений: фамилия,
период или дата, причина, источник и комментарий. Это относится и к мужчинам,
и к женщинам. Текущая фамилия и история должны быть разными сущностями. На
карточке можно показывать прежнюю фамилию в скобках, если это включено в
настройках и помещается в доступную ширину.

### Предположительные предки

Если при добавлении ребёнка указано отчество, приложение может автоматически
создать отдельные связанные карточки предполагаемого отца с именем из отчества
и фамилией ребёнка, а также предполагаемой матери с фамилией ребёнка без
выдуманного имени. Это настоящие редактируемые записи, но они получают статус
неполной/выведенной записи и источник «создано по отчеству».

При заполнении отчества у такого отца или матери можно создать следующее
предположительное поколение. Каскад выполняется по одному поколению за одну
явную операцию пользователя, с общей отменой и без бесконечного автоматического
размножения карточек.

### Даты, смерть и адрес

Дата рождения и смерти используют одну модель точности: точный день, только
год, приблизительная дата, диапазон или неизвестно. Дата смерти хранится
отдельно и включает дату, место, причину, источник и комментарий. Возраст
вычисляется с учётом точности дат; проверяется, что смерть не раньше рождения.

Место рождения хранится как исходный текст, структурированные населённый пункт,
регион и страну, а также координаты и сервис-источник, если пользователь
выбрал результат подсказки. Геокодирование является необязательной подсказкой;
ручной ввод и работа сохранённых данных не зависят от интернета.

## Приоритет 0 — целостность данных и граф связей

### Миграция и безопасность формата

- Спроектировать новую версию схемы до изменения интерфейса.
- Сохранить совместимость с текущей версией `.familytree`.
- Добавить миграцию с резервной копией, проверкой целостности и восстановлением.
- Не удалять старые сведения и не перезаписывать исходный файл без проверки.

Критерий готовности: существующие проекты открываются без потери людей, связей,
фотографий, дат и источников; повреждённый файл не перезаписывает рабочую копию.

### Единый граф отношений

- Использовать каноническую таблицу связей как источник истины.
- Обновлять родительские, детские, супружеские и братско-сестринские связи одной
  атомарной операцией.
- Рисовать все поддерживаемые типы связей: родство, брак, партнёрство,
  помолвку, усыновление, опекунство, сводное и неполнородное родство.
- Проверять симметрию, существование участников, дубли, циклы и висячие ссылки.
- Добавить безопасное удаление отдельной связи с резервной копией и отменой.

Критерий готовности: порядок создания людей не влияет на итоговый граф; связи
не исчезают после добавления родителей, супругов или новых детей; повторное
открытие файла даёт тот же результат.

### Обязательная тестовая матрица

Проверить сценарии: сначала ребёнок, потом родители; сначала братья, потом общие
родители; сначала супруги, потом ребёнок; сначала ребёнок, потом супруг родителя;
усыновление вместе с биологическими родителями; неполнородные и сводные связи;
изменение фамилии после брака; удаление человека из пары; удаление одной связи;
сохранение и повторное открытие после каждой операции; миграция и восстановление.

## Приоритет 1 — компоновка дерева

Перестроить алгоритм вокруг семейных блоков:

1. объединять супругов и партнёров в один блок;
2. размещать детей относительно центра блока родителей;
3. размещать братьев и сестёр рядом и сортировать по дате рождения;
4. использовать родительские ограничения для вертикальных поколений;
5. использовать возраст только как порядок внутри совместимого поколения;
6. размещать независимые ветви без разрыва супружеских блоков;
7. проверять пересечения карточек, линий и подписей.

Линии должны начинаться и заканчиваться на фактической границе карточки.
Размеры карточек, линий и подписей должны рассчитываться согласованно, а не по
фиксированной высоте, расходящейся с реальным содержимым.

Критерий для примера Александра и Эвелины: супруги стоят рядом, Афина
располагается по центру семейного блока, Афанасий не находится между супругами,
а братские линии не маскируются под родительскую ветку.

## Приоритет 2 — правая панель и валидация

- Сделать карандаш каждого раздела настоящей кнопкой.
- Редактировать только выбранный раздел с локальными «Сохранить» и «Отмена».
- Не закрывать панель после сохранения и не сбрасывать прокрутку.
- Валидировать каждый раздел отдельно.
- Не делать поля обязательными, но проверять формат и логические ограничения.
- Удалить кнопку «Сохранить и добавить ещё одного».

Для дат использовать маски `ДД.ММ.ГГГГ`, `ГГГГ` и два отдельных поля диапазона.
Ошибка должна появляться рядом с конкретным полем и не стирать остальные данные.

## Приоритет 3 — имена, фамилии и автоматическое заполнение

- Перейти на структурированные части ФИО.
- Добавить историю фамилий для всех полов.
- Показывать прежние фамилии в панели и по настройке на карточке.
- При добавлении ребёнка предварительно заполнять фамилию по обоим родителям,
  разрешая пользователю изменить результат.
- Учитывать брак, помолвку, партнёрство и усыновление.
- Сохранять источник и статус автоматически выведенных имён.

## Приоритет 4 — визуальные обозначения

- Синяя граница только у обычных карточек мужчин.
- Розовая граница только у обычных карточек женщин.
- Карточки без указанного пола не окрашивать.
- Не заменять цвет пола синей рамкой выбранного человека.
- Подписывать все линии кратким типом связи.
- По нажатию на подпись показывать полное название родства.
- Добавить номер ребёнка в угол карточки и в список детей.
- Нумеровать детей по дате рождения, сохраняя ручную корректировку.

## Приоритет 5 — родственная ветвь

Заменить упрощённый режим «Ближайшая семья» режимом отображения ветви:

- выбранный человек и его кровные родственники через любое число поколений
  остаются полностью видимыми;
- остальные люди становятся полупрозрачными;
- супруги и близкие родственники включаются по понятному правилу;
- можно ограничить глубину одним или несколькими поколениями;
- одной кнопкой возвращается полное дерево.

## Приоритет 6 — калькулятор родства

- Строить отображаемый путь от старших поколений к младшим.
- Сначала показывать родителей и предков, затем человека и детей.
- Внутри поколения использовать возраст и порядок детей.
- Не зависеть от порядка выбора двух людей.
- Показывать краткое название, полный путь и тип каждой связи.

## Приоритет 7 — дата смерти

- Добавить дату, место, причину, источник и комментарий.
- Поддержать все варианты точности дат.
- Проверять хронологию рождения и смерти.
- Рассчитывать возраст с учётом приблизительности.
- Показывать значок смерти и событие во временной шкале.

## Приоритет 8 — поиск адреса

- Добавить подсказки населённых пунктов и адресов через API геокодера.
- Использовать адаптер провайдера; начальный кандидат — Яндекс.
- Предусмотреть ключ, ограничение запросов и кэш подсказок.
- При отсутствии сети оставить ручной ввод полностью рабочим.
- Не отправлять адрес во внешний сервис без понятного действия пользователя.

## Приоритет 9 — дополнительные улучшения качества

- Добавить происхождение сведения: ручное, импортированное или выведенное.
- Добавить журнал изменений человека и связей.
- Добавить отмену последнего изменения связей.
- Добавить предупреждения о противоречивых датах, фамилиях и родстве.
- Добавить сворачивание ветвей и центрирование семейной пары.
- Синхронизировать экранные настройки с PDF/PNG/TIFF-экспортом.
- Проверить клавиатуру, фокус, экранный диктор, крупный текст и контраст.
- Проверить производительность на больших деревьях после появления подписей и
  истории фамилий.

## Новый согласованный микроэтап P1.4 — подписи брака

Статус: **завершён 25.08.2026**.

- размещать раскрытые подписи брака сверху линии и над карточками пары;
- учитывать полный текст при расчёте свободного канала даже в скрытом режиме;
- при раскрытии сдвигать вниз всю пару и последующие поколения единым блоком;
- использовать одинаковую геометрию на полотне и в экспорте.

## Новый согласованный микроэтап P1.5 — глубина родственной ветви

Статус: **завершён 25.08.2026**.

- показывать подпись «Глубина» над выбором числа поколений;
- ограничить выбор диапазоном от 1 до 10 поколений;
- сохранять значение в `project.settings.branchDepth` и восстанавливать его
  при открытии и восстановлении проекта;
- увеличить поле режима ветви и центрировать его содержимое по вертикали и
  горизонтали;
- для старых проектов без настройки использовать значение 10 поколений.

## Новый согласованный микроэтап P1.6 — стартовое меню

Статус: **реализован 25.08.2026; визуальная проверка ожидает запуска на Windows с рабочим GPU**.

- запускать окно Electron в настоящем системном полноэкранном режиме без рамки
  Windows;
- показывать главное меню плавным появлением снизу экрана;
- добавить выразительный декоративный фон: ветви, листья, линии связей,
  полупрозрачные карточки и частицы;
- при `prefers-reduced-motion` отключать только фоновые бесконечные движения,
  сохраняя плавное появление самого меню;
- дать пользователю кнопку «Пропустить анимацию», которая сразу завершает
  появление меню и убирает фоновую анимацию;
- не менять формат `.familytree`, данные пользователей и порядок действий
  основного меню.

## Новый согласованный микроэтап P1.7 — верхние подписи и выразительный вход

Статус: **реализован 26.08.2026**.

- размещать короткую и раскрытую подпись брака над верхним левым краем пары;
- применять один и тот же якорь на полотне и в экспорте;
- замедлить вход стартового меню до плавного подъёма снизу с мягкой остановкой
  в центре;
- оставить ветви, листья, карточки, линии и частицы в циклической фоновой
  анимации после завершения входа;
- сохранить кнопку пропуска и режим уменьшения движения.

## Правило завершения этапа

Этап считается завершённым только после реализации, миграционного теста при
изменении формата, модульных и сценарных тестов, открытия и повторного сохранения
реального `.familytree`, визуальной проверки, проверки восстановления и отмены,
а также документации результата в `docs/stage-*.md`.

## Завершённый предыдущий блок — UI и UX

Предыдущий блок закрыт. Новый согласованный блок UI/UX описан в приоритетах 2,
4, 5 и 9 выше.

## Завершённый предыдущий блок — экспорт и производительность

Предыдущий блок закрыт. Новые требования к производительности, экспорту и
доступности добавлены в приоритет 9.

## История согласованных решений

25.08.2026 пользователь утвердил:

- структурированное ФИО вместо проверки на ровно три слова;
- историю фамилий для мужчин и женщин;
- предположительное автоматическое создание родителей по отчеству с отметкой
  происхождения;
- строгую проверку противоречий без обязательного заполнения всех полей;
- поэтапную реализацию от целостности данных и связей к интерфейсу и внешним
  сервисам;
- дополнительные предложения по тестам, происхождению данных, отмене,
  сворачиванию ветвей, доступности и соответствию экспорта.

## Завершённый предыдущий блок — улучшение комфорта

Раздел закрыт: крупный текстовый режим и базовая доступность основных действий
реализованы и проверены. Новые задачи добавляются только после отдельного решения.

## Исключено по решению пользователя

- Сертификаты и подписи установщика или обновлений.
